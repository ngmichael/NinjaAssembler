package nja.lexer

import scala.collection.mutable.ListBuffer

object Lexer {
  def generateTokenList(codeLines: Iterator[String]): List[Token] = {
    var tokens: ListBuffer[Token] = ListBuffer.empty

    tokens.toList
  }
}
